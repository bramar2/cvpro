export function getDefaultConfig(): Record<string, boolean> {
    return {
        "config-premium-elements": true,
        "config-hide-pro-trial": true,
        "config-remove-background": true
    };
}